<Window x:Class="CBDownloader.Views.PlaylistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Playlist" Width="560" Height="540"
        MinHeight="360" ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Icon="/Assets/icon.ico"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="IndexTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="26"/>
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Loading Overlay -->
        <Border Background="{DynamicResource WindowBackgroundBrush}"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                Panel.ZIndex="10">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⏳" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="{Binding LoadingStatus}" FontSize="13"
                           Foreground="{DynamicResource WindowForegroundBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Margin="16" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <!-- Invisible placeholder to keep layout during load -->
        </Grid>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="{Binding PlaylistTitle}" FontSize="15" FontWeight="Bold"
                           Foreground="{DynamicResource WindowForegroundBrush}"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock FontSize="11" Margin="0,3,0,0"
                           Foreground="{DynamicResource SecondaryForegroundBrush}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}Select videos to download  •  {0}/{1} selected">
                            <Binding Path="SelectedCount"/>
                            <Binding Path="TotalCount"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>

            <!-- Toolbar -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Select All" Width="90" Height="26" Margin="0,0,6,0"
                        Command="{Binding SelectAllCommand}"/>
                <Button Content="Deselect All" Width="95" Height="26"
                        Command="{Binding DeselectAllCommand}"/>
            </StackPanel>

            <!-- Video List -->
            <Border Grid.Row="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3">
                <ListView ItemsSource="{Binding Entries}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          Background="Transparent" BorderThickness="0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="6,4"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ButtonHoverBackgroundBrush}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="54"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}"
                                          VerticalAlignment="Center" Margin="0,0,8,0"/>

                                <TextBlock Grid.Column="1" Text="{Binding Index}"
                                           Style="{StaticResource IndexTextStyle}"/>

                                <Border Grid.Column="2" Width="52" Height="34" CornerRadius="2"
                                        Background="{DynamicResource ButtonBackgroundBrush}"
                                        Margin="0,0,8,0">
                                    <Image Source="{Binding ThumbnailUrl}" Stretch="UniformToFill"/>
                                </Border>

                                <TextBlock Grid.Column="3" Text="{Binding Title}"
                                           FontSize="12" TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource WindowForegroundBrush}"/>

                                <TextBlock Grid.Column="4" Text="{Binding Duration}"
                                           FontSize="11" VerticalAlignment="Center"
                                           Margin="8,0,0,0"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <!-- Footer -->
            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="11"
                           Foreground="{DynamicResource SecondaryForegroundBrush}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0} video(s) will be queued">
                            <Binding Path="SelectedCount"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <Button Grid.Column="1" Content="Cancel" Width="80" Height="32" Margin="0,0,8,0"
                        Command="{Binding CancelCommand}"/>
                <Button Grid.Column="2" Content="⬇️ Download Selected" Height="32" Padding="14,0"
                        Command="{Binding StartDownloadCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedCount}" Value="0">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
